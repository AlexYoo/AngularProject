<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hist.demo.mapper.ReviewMapper">
	<insert id="insertReview" parameterType="com.hist.demo.vo.Review">
		INSERT INTO review 
		(
			user_id,
			store_id,
			contents,
			rating,
			REG_ID,
			UPT_ID
		)
		VALUES 
		(
			#{userId},
			#{storeId},
			#{contents},
			#{rating},
			#{regId},
			#{uptId}
		)
	</insert>
	
</mapper>

